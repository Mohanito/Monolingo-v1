<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monolingo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
</head>

<body>
    <div class="d-flex container w-100 h-100">
        <div class="col-6 offset-3 text-center">
            <h3>Monolingo</h3>
            <p>We understand that learning a second language is hard. That's why we designed Monolingo, a video web app
                with real-time translation, to help you communicate with people around the world, using your mother
                language.
            </p>
            <h3>Start using now!</h3>
            <p>Generate your random username to start video chat.</p>
            <form action="/room">
                <p id="username" name="username">Generated Username Here</p>
                <button class="btn btn-success" id="dice">Generate New Username</button>
                <button class="btn btn-success">Join Random Chat</button>
            </form>
        </div>
    </div>
</body>

<script>
    const dice = document.querySelector('#dice');
    const adjs = ['Grumpy', 'Old', 'Angry', 'Silent'];
    const animals = ['Cat', 'Dog', 'Duck', 'Chicken'];
    dice.addEventListener('click', (event) => {
        event.preventDefault();
        const username = document.querySelector('#username');
        const adj = adjs[Math.floor(Math.random() * adjs.length)];
        const animal = animals[Math.floor(Math.random() * animals.length)];
        username.innerHTML = adj + ' ' + animal;
    })
</script>

</html>